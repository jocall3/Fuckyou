// --- SEGMENT 2 CONSOLIDATION: MetricCard componentimport React, { useState, useMemo } from 'react';

export

// --- SEGMENT 2 CONSOLIDATION: MetricCard componentimport React, { useState, useMemo } from 'react';

export

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title,import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title,import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-90import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | '

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-90import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | '

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-centerimport React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle:

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-centerimport React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle:

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs fontimport React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enroll

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs fontimport React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enroll

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;


// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;


// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetry

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetry

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          classNameimport React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          classNameimport React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetry

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-5import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-5import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-5import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-5import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="textimport React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor"

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor"

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers:

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (


// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload:

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (


// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10"

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10" />
    <line x1="12" y1="15" x2="12" y

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10" />
    <line x1="12" y1="15" x2="12" y2="3" />
  </svg>
);

export const IconRefresh: React.FC<{ className?:

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10" />
    <line x1="12" y1="15" x2="12" y2="3" />
  </svg>
);

export const IconRefresh: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10" />
    <line x1="12" y1="15" x2="12" y2="3" />
  </svg>
);

export const IconRefresh: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10" />
    <line x1="12" y1="15" x2="12" y2="3" />
  </svg>
);

export const IconRefresh: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M23 4v6h-6" />
    <path

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10" />
    <line x1="12" y1="15" x2="12" y2="3" />
  </svg>
);

export const IconRefresh: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M23 4v6h-6" />
    <path d="M1 20v-6h6" />
    <path d="M3.5

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10" />
    <line x1="12" y1="15" x2="12" y2="3" />
  </svg>
);

export const IconRefresh: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M23 4v6h-6" />
    <path d="M1 20v-6h6" />
    <path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 1

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10" />
    <line x1="12" y1="15" x2="12" y2="3" />
  </svg>
);

export const IconRefresh: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M23 4v6h-6" />
    <path d="M1 20v-6h6" />
    <path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0 0 20

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;
import React, { useState, useMemo } from 'react';

export type TimeRange = '24H' | '7D' | '30D' | '90D' | 'YTD';
export type ChannelFilter = 'ALL' | 'MOBILE_APP' | 'WEB' | 'MERCHANT_POS' | 'PARTNER_API';

export interface MetricCardProps {
  title: string;
  value: string;
  delta: string;
  isPositive: boolean;
  subtitle: string;
  icon: React.ReactNode;
}

export interface TrendDataPoint {
  date: string;
  enrollments: number;
  unenrollments: number;
  pointsBurned: string;
}

export interface ProductDistributionItem {
  code: string;
  name: string;
  share: number;
  count: string;
  color: string;
  rpc: string;
}

export interface ChannelTelemetryItem {
  channel: ChannelFilter;
  label: string;
  count: number;
  percentage: number;
  conversionRate: string;
  avgLatency: string;
}

export interface ErrorTelemetryItem {
  code: string;
  title: string;
  description: string;
  occurrences: number;
  rate: string;
}

export interface HoveredPointData extends TrendDataPoint {
  x: number;
  y: number;
}

export const IconTrendingUp: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18" />
    <polyline points="17 6 23 6 23 12" />
  </svg>
);

export const IconShieldCheck: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
    <polyline points="9 12 11 14 15 10" />
  </svg>
);

export const IconActivity: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
  </svg>
);

export const IconCreditCard: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <rect x="1" y="4" width="22" height="16" rx="2" ry="2" />
    <line x1="1" y1="10" x2="23" y2="10" />
  </svg>
);

export const IconUsers: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
    <circle cx="9" cy="7" r="4" />
    <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
    <path d="M16 3.13a4 4 0 0 1 0 7.75" />
  </svg>
);

export const IconDownload: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="7 10 12 15 17 10" />
    <line x1="12" y1="15" x2="12" y2="3" />
  </svg>
);

export const IconRefresh: React.FC<{ className?: string }> = ({ className = 'w-4 h-4' }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path d="M23 4v6h-6" />
    <path d="M1 20v-6h6" />
    <path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0 0 20.49 15" />
  </svg>
);

// --- SEGMENT 2 CONSOLIDATION: MetricCard component, hook handlers, export declarations, and clean file termination ---

const MetricCard: React.FC<MetricCardProps> = ({ title, value, delta, isPositive, subtitle, icon }) => {
  return (
    <div className="bg-slate-900/80 rounded-2xl p-5 border border-slate-800/90 shadow-lg hover:border-slate-700 transition-all">
      <div className="flex items-center justify-between text-slate-400 mb-3">
        <span className="text-xs font-medium uppercase tracking-wider">{title}</span>
        <div className="p-2 rounded-xl bg-slate-800/80 border border-slate-700/60">{icon}</div>
      </div>
      <div className="flex items-baseline justify-between">
        <span className="text-2xl font-extrabold text-white tracking-tight">{value}</span>
        <span
          className={`text-xs font-bold px-2 py-0.5 rounded-md flex items-center gap-1 ${
            isPositive
              ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-400 border border-rose-500/20'
          }`}
        >
          {delta}
        </span>
      </div>
      <p className="text-xs text-slate-400 mt-2">{subtitle}</p>
    </div>
  );
};

export default ChaseAnalyticsPanel;