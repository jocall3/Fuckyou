import Reactimport React

            {import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 

            {import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 

            {activeTab === 'inspector' ? (
              <div className="flex flex-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server

            {activeTab === 'inspector' ? (
              <div className="flex flex-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Tokenimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from '

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Tokenimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from '

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label classNameimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label classNameimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;


            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;


            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string,

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string,

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId:

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId:

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14}import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId:

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;


            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;


            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b]import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title:

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b]import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title:

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.splitimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.splitimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="textimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="textimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span classNameimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span classNameimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scopeimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scopeimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <divimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS25

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <divimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS25

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-4import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-4import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-4import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-colimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-colimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <divimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <divimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-4import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-4import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-colimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-colimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () =>

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () =>

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] borderimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] borderimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-yimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-yimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 fleximport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header:

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 fleximport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header:

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header:

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, iimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, iimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (e) {
    return null;
  }
};

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (e) {
    return null;
  }
};

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.includes('→')) colorClass = 'text-blue-400';
                      if (log.includes('import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (e) {
    return null;
  }
};

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.includes('→')) colorClass = 'text-blue-400';
                      if (log.includes('←')) colorClass = 'text-emerald-400';
                      if (log.includes('✖')import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (e) {
    return null;
  }
};

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.includes('→')) colorClass = 'text-blue-400';
                      if (log.includes('←')) colorClass = 'text-emerald-400';
                      if (log.includes('✖') || log.includes('400')) colorClass = 'text-red-400';
                      ifimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (e) {
    return null;
  }
};

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.includes('→')) colorClass = 'text-blue-400';
                      if (log.includes('←')) colorClass = 'text-emerald-400';
                      if (log.includes('✖') || log.includes('400')) colorClass = 'text-red-400';
                      if (log.includes('ℹ')) colorClass = 'text-amber-400';
                      
                      returnimport React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (e) {
    return null;
  }
};

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.includes('→')) colorClass = 'text-blue-400';
                      if (log.includes('←')) colorClass = 'text-emerald-400';
                      if (log.includes('✖') || log.includes('400')) colorClass = 'text-red-400';
                      if (log.includes('ℹ')) colorClass = 'text-amber-400';
                      
                      return (
                        <div key={i} className={`${colorClass} break-all`}>
                          {log}import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (e) {
    return null;
  }
};

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.includes('→')) colorClass = 'text-blue-400';
                      if (log.includes('←')) colorClass = 'text-emerald-400';
                      if (log.includes('✖') || log.includes('400')) colorClass = 'text-red-400';
                      if (log.includes('ℹ')) colorClass = 'text-amber-400';
                      
                      return (
                        <div key={i} className={`${colorClass} break-all`}>
                          {log}
                        </div>
                      );
                    })}
                    <div ref={logsEndRef} />
                  </div>
import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (e) {
    return null;
  }
};

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.includes('→')) colorClass = 'text-blue-400';
                      if (log.includes('←')) colorClass = 'text-emerald-400';
                      if (log.includes('✖') || log.includes('400')) colorClass = 'text-red-400';
                      if (log.includes('ℹ')) colorClass = 'text-amber-400';
                      
                      return (
                        <div key={i} className={`${colorClass} break-all`}>
                          {log}
                        </div>
                      );
                    })}
                    <div ref={logsEndRef} />
                  </div>
                )}
              </div>
            )}
          </Card>
        </div>
      </div>
    </div>
  import React, { useState, useEffect, useCallback, useRef } from 'react';
import { 
  Shield, Key, Clock, CheckCircle, AlertCircle, 
  RefreshCw, Copy, Eye, EyeOff, Server, Lock, 
  Code, Activity, ChevronRight, Terminal, Check,
  Zap, Database, Globe
} from 'lucide-react';

export interface TokenResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  scope: string;
  id_token?: string;
}

export interface DecodedToken {
  header: Record<string, any>;
  payload: Record<string, any>;
}

export interface AuthConfig {
  clientId: string;
  clientSecret: string;
  authCode: string;
  redirectUri: string;
  grantType: string;
}

export interface CardProps {
  children: React.ReactNode;
  title: string;
  icon: React.ElementType;
  className?: string;
  action?: React.ReactNode;
}

export interface InputGroupProps {
  label: string;
  type?: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  icon: React.ElementType;
  isSecret?: boolean;
}

export interface JsonViewerProps {
  data: any;
}

export const generateUuid = (): string => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0, v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};

export const base64UrlEncode = (obj: any): string => {
  try {
    return btoa(JSON.stringify(obj)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  } catch (e) {
    return '';
  }
};

export const generateMockJwt = (clientId: string, scopes: string, expiresIn: number): string => {
  const header = { alg: 'RS256', typ: 'JWT', kid: 'chase-auth-key-prod-1' };
  const payload = {
    iss: 'https://api.chase.com/identity/auth/v1',
    sub: `usr_${generateUuid().substring(0, 8)}`,
    aud: clientId || 'chase_partner_client',
    exp: Math.floor(Date.now() / 1000) + expiresIn,
    iat: Math.floor(Date.now() / 1000),
    jti: generateUuid(),
    scope: scopes,
    amr: ['pwd', 'mfa'],
    client_id: clientId
  };
  
  const signature = Array.from({length: 43}, () => 
    'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'[Math.floor(Math.random() * 64)]
  ).join('');

  return `${base64UrlEncode(header)}.${base64UrlEncode(payload)}.${signature}`;
};

export const decodeJwt = (token: string): DecodedToken | null => {
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const decode = (str: string) => JSON.parse(atob(str.replace(/-/g, '+').replace(/_/g, '/')));
    return {
      header: decode(parts[0]),
      payload: decode(parts[1])
    };
  } catch (e) {
    return null;
  }
};

            {activeTab === 'inspector' ? (
              <div className="flex flex-col h-full gap-4">
                {tokenData ? (
                  <>
                    {/* Raw Token */}
                    <div>
                      <div className="flex items-center justify-between mb-2">
                        <label className="text-xs font-medium text-slate-400">Raw Access Token</label>
                        <button 
                          onClick={() => copyToClipboard(tokenData.access_token)}
                          className="text-xs flex items-center gap-1 text-blue-400 hover:text-blue-300 transition-colors"
                        >
                          {copiedToken ? <Check size={14} /> : <Copy size={14} />}
                          {copiedToken ? 'Copied!' : 'Copy'}
                        </button>
                      </div>
                      <div className="bg-[#0b1120] border border-[#1e293b] rounded-lg p-3 font-mono text-xs break-all text-slate-400 leading-relaxed">
                        <span className="text-pink-400">{tokenData.access_token.split('.')[0]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-purple-400">{tokenData.access_token.split('.')[1]}</span>
                        <span className="text-slate-500">.</span>
                        <span className="text-emerald-400">{tokenData.access_token.split('.')[2]}</span>
                      </div>
                    </div>

                    {/* Scopes */}
                    <div>
                      <label className="text-xs font-medium text-slate-400 mb-2 block">Granted Scopes</label>
                      <div className="flex flex-wrap gap-2">
                        {tokenData.scope.split(' ').map(scope => (
                          <span key={scope} className="px-2.5 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs rounded-md font-mono">
                            {scope}
                          </span>
                        ))}
                      </div>
                    </div>

                    {/* Decoded JWT */}
                    <div className="flex-1 grid grid-cols-2 gap-4 min-h-[200px]">
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Header (JOSE)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.header} />
                        </div>
                      </div>
                      <div className="flex flex-col">
                        <label className="text-xs font-medium text-slate-400 mb-2 block">Payload (Claims)</label>
                        <div className="flex-1 overflow-hidden flex flex-col">
                          <JsonViewer data={decodedToken?.payload} />
                        </div>
                      </div>
                    </div>
                  </>
                ) : (
                  <div className="flex-1 flex flex-col items-center justify-center text-slate-500 gap-3">
                    <Shield size={48} className="opacity-20" />
                    <p className="text-sm">Execute a token exchange to inspect the JWT payload.</p>
                  </div>
                )}
              </div>
            ) : (
              <div className="flex-1 bg-[#0b1120] border border-[#1e293b] rounded-lg p-4 font-mono text-xs overflow-y-auto flex flex-col">
                {logs.length === 0 ? (
                  <div className="flex-1 flex items-center justify-center text-slate-600">
                    No network activity yet.
                  </div>
                ) : (
                  <div className="space-y-1.5">
                    {logs.map((log, i) => {
                      let colorClass = 'text-slate-400';
                      if (log.includes('→')) colorClass = 'text-blue-400';
                      if (log.includes('←')) colorClass = 'text-emerald-400';
                      if (log.includes('✖') || log.includes('400')) colorClass = 'text-red-400';
                      if (log.includes('ℹ')) colorClass = 'text-amber-400';
                      
                      return (
                        <div key={i} className={`${colorClass} break-all`}>
                          {log}
                        </div>
                      );
                    })}
                    <div ref={logsEndRef} />
                  </div>
                )}
              </div>
            )}
          </Card>
        </div>
      </div>
    </div>
  );
}