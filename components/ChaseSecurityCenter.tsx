//import

//import

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// Thisimport React, { useState, useEffect, useCallback } from 'react';
import {
  

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// Thisimport React, { useState, useEffect, useCallback } from 'react';
import {
  

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporterimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  Alert

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporterimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  Alert

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,


// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,


// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  token

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  token

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;


// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: '

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status:

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bgimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check classNameimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy classNameimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-8import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-ximport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -Ximport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/cardimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-pointsimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span classNameimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0,import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0, 32)}..."</span> \<br />
                      {'  '}-H <span className="text-amber-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0, 32)}..."</span> \<br />
                      {'  '}-H <span className="text-amber-300">"trace-id: {credentials.traceId}"</span> \<br />
                      {'  'import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0, 32)}..."</span> \<br />
                      {'  '}-H <span className="text-amber-300">"trace-id: {credentials.traceId}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"enrollment-type-code: ENROLLimport React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0, 32)}..."</span> \<br />
                      {'  '}-H <span className="text-amber-300">"trace-id: {credentials.traceId}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"enrollment-type-code: ENROLL"</span> \<br />
                      {'  '}-H <span className="text-amber-300">import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0, 32)}..."</span> \<br />
                      {'  '}-H <span className="text-amber-300">"trace-id: {credentials.traceId}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"enrollment-type-code: ENROLL"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"external-account-identifier: {credentials.externalAccountIdentifier}"</span> \<br />
                      {'  '}-import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0, 32)}..."</span> \<br />
                      {'  '}-H <span className="text-amber-300">"trace-id: {credentials.traceId}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"enrollment-type-code: ENROLL"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"external-account-identifier: {credentials.externalAccountIdentifier}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"channel-type: {credentials.channelType}"import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0, 32)}..."</span> \<br />
                      {'  '}-H <span className="text-amber-300">"trace-id: {credentials.traceId}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"enrollment-type-code: ENROLL"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"external-account-identifier: {credentials.externalAccountIdentifier}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"channel-type: {credentials.channelType}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0, 32)}..."</span> \<br />
                      {'  '}-H <span className="text-amber-300">"trace-id: {credentials.traceId}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"enrollment-type-code: ENROLL"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"external-account-identifier: {credentials.externalAccountIdentifier}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"channel-type: {credentials.channelType}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"Content-Type: application/json"</span>
                    </code>
                  </pre>
                </div>
              </div>
import React, { useState, useEffect, useCallback } from 'react';
import {
  Shield,
  Lock,
  Key,
  RefreshCw,
  CheckCircle2,
  AlertTriangle,
  Terminal,
  Copy,
  Check,
  ArrowRight,
  Zap,
  Eye,
  EyeOff,
  Server,
  Globe,
  Cpu,
  Layers,
  Activity,
  FileCode,
  Fingerprint,
  Radio,
  Clock
} from 'lucide-react';

export interface SecurityCredentials {
  clientId: string;
  clientSecret: string;
  scope: string;
  tokenUrl: string;
  bearerToken: string;
  tokenExpiresIn: number;
  tokenGeneratedAt: number | null;
  authorization2Token: string;
  traceId: string;
  externalAccountIdentifier: string;
  accountReferenceUuid: string;
  channelType: 'WEB' | 'MOBILE_APP' | 'MOBILE_WEB' | 'BATCH';
}

export interface SecurityAuditLog {
  id: string;
  timestamp: string;
  type: 'AUTH' | 'TOKEN' | 'SIGNATURE' | 'TRACE' | 'GATEWAY';
  status: 'SUCCESS' | 'WARN' | 'ERROR';
  message: string;
  details: Record<string, any>;
}

// [SEGMENT 2 CONTINUATION & COMPLETION FOR ChaseSecurityCenter.tsx]
// This segment completes the component rendering, hook handlers, export declarations, and clean file termination.

                {/* cURL Live Exporter Box */}
                <div className="bg-[#060c14] border-t border-[#1a2e46] p-5">
                  <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center space-x-2">
                      <Terminal className="w-4 h-4 text-[#41B6E6]" />
                      <span className="text-xs font-semibold text-slate-200 uppercase tracking-wider">
                        Ready-to-Execute cURL Blueprint
                      </span>
                    </div>
                    <button
                      onClick={() => {
                        const curl = `curl -X POST "https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/${credentials.accountReferenceUuid}" \\
            -H "authorization: Bearer ${credentials.bearerToken}" \\
            -H "trace-id: ${credentials.traceId}" \\
            -H "enrollment-type-code: ENROLL" \\
            -H "external-account-identifier: ${credentials.externalAccountIdentifier}" \\
            -H "channel-type: ${credentials.channelType}" \\
            -H "Content-Type: application/json"`;
                        handleCopy(curl, 'curl');
                      }}
                      className="flex items-center space-x-1 px-2.5 py-1 text-xs bg-[#122438] hover:bg-[#1a3554] text-[#41B6E6] border border-[#214269] rounded transition-all"
                    >
                      {copiedKey === 'curl' ? <Check className="w-3.5 h-3.5 text-emerald-400" /> : <Copy className="w-3.5 h-3.5" />}
                      <span>Copy cURL Command</span>
                    </button>
                  </div>

                  <pre className="p-3 bg-[#03070d] border border-slate-800 rounded-lg text-[11px] font-mono text-slate-300 overflow-x-auto">
                    <code>
                      <span className="text-purple-400">curl</span> -X POST <span className="text-emerald-300">"https://api.chase.com/card/loyalty/earn-rewards/enrollment/v1/merchants/programs/pay-with-points/enrollments/{credentials.accountReferenceUuid}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"authorization: Bearer {credentials.bearerToken.substring(0, 32)}..."</span> \<br />
                      {'  '}-H <span className="text-amber-300">"trace-id: {credentials.traceId}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"enrollment-type-code: ENROLL"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"external-account-identifier: {credentials.externalAccountIdentifier}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"channel-type: {credentials.channelType}"</span> \<br />
                      {'  '}-H <span className="text-amber-300">"Content-Type: application/json"</span>
                    </code>
                  </pre>
                </div>
              </div>
            );
          };

          export default ChaseSecurityCenter;