</div>    </div>
  );
};

export default AccountList;import React

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  Message

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 


    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?:

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLogin

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string |

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details:

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (date

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof date

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

export const getStatusColorClasses = (status: string): string => {
  switch

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

export const getStatusColorClasses = (status: string): string => {
  switch (status?.toLowerCase()) {
    case 'active':
      return 'text-emerald-400 bg-

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

export const getStatusColorClasses = (status: string): string => {
  switch (status?.toLowerCase()) {
    case 'active':
      return 'text-emerald-400 bg-emerald-950/30 border border-emerald-500/20';
    case 'pending':
      

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

export const getStatusColorClasses = (status: string): string => {
  switch (status?.toLowerCase()) {
    case 'active':
      return 'text-emerald-400 bg-emerald-950/30 border border-emerald-500/20';
    case 'pending':
      return 'text-amber-400 bg-amber-950/30 border border-amber-

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

export const getStatusColorClasses = (status: string): string => {
  switch (status?.toLowerCase()) {
    case 'active':
      return 'text-emerald-400 bg-emerald-950/30 border border-emerald-500/20';
    case 'pending':
      return 'text-amber-400 bg-amber-950/30 border border-amber-500/20';
    case 'frozen':
      return 'text-red-400

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

export const getStatusColorClasses = (status: string): string => {
  switch (status?.toLowerCase()) {
    case 'active':
      return 'text-emerald-400 bg-emerald-950/30 border border-emerald-500/20';
    case 'pending':
      return 'text-amber-400 bg-amber-950/30 border border-amber-500/20';
    case 'frozen':
      return 'text-red-400 bg-red-950/30 border border-red-500/20';
    

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

export const getStatusColorClasses = (status: string): string => {
  switch (status?.toLowerCase()) {
    case 'active':
      return 'text-emerald-400 bg-emerald-950/30 border border-emerald-500/20';
    case 'pending':
      return 'text-amber-400 bg-amber-950/30 border border-amber-500/20';
    case 'frozen':
      return 'text-red-400 bg-red-950/30 border border-red-500/20';
    case 'closed':
      return 'text-gray-400 bg-gray-800/30 border

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

export const getStatusColorClasses = (status: string): string => {
  switch (status?.toLowerCase()) {
    case 'active':
      return 'text-emerald-400 bg-emerald-950/30 border border-emerald-500/20';
    case 'pending':
      return 'text-amber-400 bg-amber-950/30 border border-amber-500/20';
    case 'frozen':
      return 'text-red-400 bg-red-950/30 border border-red-500/20';
    case 'closed':
      return 'text-gray-400 bg-gray-800/30 border border-gray-700/20';
    default:
      return 'text-cyan-400 bg-cyan

    </div>
  );
};

export default AccountList;import React, { useState, useEffect, useRef } from 'react';
import { 
  Landmark, 
  ArrowRight, 
  ShieldCheck, 
  Activity, 
  MessageSquare, 
  X, 
  Send, 
  Terminal, 
  Database, 
  Car, 
  Key, 
  FileText, 
  Save, 
  AlertTriangle, 
  RefreshCw 
} from 'lucide-react';
import { GoogleGenAI } from "@google/genai";

// --- Types & Interfaces ---

export interface CustomerAccount {
  id: string;
  externalId?: string;
  accountNumberDisplay: string;
  name: string;
  institutionName?: string;
  balance: number;
  availableBalance?: number;
  type: string;
  subtype?: string;
  mask?: string;
  status: string;
  institutionId?: string;
  customerId?: string;
  balanceDate?: number;
  createdDate?: number;
  currency?: string;
  institutionLoginId?: number;
  lastUpdated?: string;
}

export interface AccountListProps {
  accounts: CustomerAccount[];
  onAccountSelect?: (accountId: string) => void;
  isLoading?: boolean;
  error?: string | Error | null;
  onRetry?: () => void;
}

export interface ChatMessage {
  role: 'user' | 'model' | 'system';
  text: string;
  timestamp: number;
}

export interface AuditRecord {
  id: string;
  action: string;
  details: string;
  timestamp: string;
  user: string;
}

// --- Context & Configuration ---

export const CONTEXT_STORY = `
You are the AI assistant for a 32-year-old visionary who took a global bank's framework and transformed it into this "Demo Company" based on an interpretation of terms and conditions. 
The user is "test-driving" this financial vehicle. It's like test-driving a car – they get to kick the tires, see all the bells and whistles.
This platform is a secret weapon. It allows users to virtually walk through the entire platform.
Key features: Efficiency, clarity, international payments, fraud protection, cash flow insights.
The user experience is paramount. 
Do NOT use the name "Citibank". Refer to it as "The Global Demo Platform" or "The Engine".
If asked about the founder: He is 32, read the cryptic messages and an EIN 2021, and built this monolith.
`;

// --- Helper Functions for Formatting & Display ---

export const formatCurrency = (amount: number | undefined, currencyCode: string = 'USD'): string => {
  if (typeof amount !== 'number') {
    return 'N/A';
  }
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currencyCode,
  }).format(amount);
};

export const formatDate = (dateInput: string | number | undefined): string => {
  if (dateInput === undefined) {
    return 'N/A';
  }

  let date: Date;
  if (typeof dateInput === 'number') {
    date = new Date(dateInput * 1000);
  } else {
    date = new Date(dateInput);
  }

  if (isNaN(date.getTime())) {
    return 'N/A';
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

export const getStatusColorClasses = (status: string): string => {
  switch (status?.toLowerCase()) {
    case 'active':
      return 'text-emerald-400 bg-emerald-950/30 border border-emerald-500/20';
    case 'pending':
      return 'text-amber-400 bg-amber-950/30 border border-amber-500/20';
    case 'frozen':
      return 'text-red-400 bg-red-950/30 border border-red-500/20';
    case 'closed':
      return 'text-gray-400 bg-gray-800/30 border border-gray-700/20';
    default:
      return 'text-cyan-400 bg-cyan-950/30 border border-cyan-500/20';
  }
};

    </div>
  );
};

export default AccountList;